import SwiftUI

struct PlantCameraView: View {
    
    var body: some View {
        VStack {
            Text("plant camera view").bold()
        }
        
    }
}

#Preview {
    PlantCameraView()
}
